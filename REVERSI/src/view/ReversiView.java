package view;

/**
 * Represents a view visually representing the Reversi Board.
 * This view shows each tile of the board and any moves that have been made.
 */
public interface ReversiView {
  // Since this interface can represent many views,
  // it currently does not contain any methods neeeded across all view represetations.
}
